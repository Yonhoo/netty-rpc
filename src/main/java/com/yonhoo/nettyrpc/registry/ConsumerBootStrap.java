package com.yonhoo.nettyrpc.registry;

public class ConsumerBootStrap {
    private ProviderInfoListener providerInfoListener = new DefaultProviderInfoListener();
    private ConsumerConfig consumerConfig;

    public static <T> T from(ConsumerConfig consumerConfig) {
        return null;
    }
}
